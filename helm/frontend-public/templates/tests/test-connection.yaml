apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-public.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-public.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-public.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
